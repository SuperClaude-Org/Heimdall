diff --git a/vendor/opencode/README.md b/vendor/opencode/README.md
index 30c0a7eb..1e16bde8 100644
--- a/vendor/opencode/README.md
+++ b/vendor/opencode/README.md
@@ -37,14 +37,14 @@ paru -S opencode-bin               # Arch Linux
 
 The install script respects the following priority order for the installation path:
 
-1. `$HEIMDALL_INSTALL_DIR` - Custom installation directory
+1. `$OPENCODE_INSTALL_DIR` - Custom installation directory
 2. `$XDG_BIN_DIR` - XDG Base Directory Specification compliant path
 3. `$HOME/bin` - Standard user binary directory (if exists or can be created)
 4. `$HOME/.opencode/bin` - Default fallback
 
 ```bash
 # Examples
-HEIMDALL_INSTALL_DIR=/usr/local/bin curl -fsSL https://opencode.ai/install | bash
+OPENCODE_INSTALL_DIR=/usr/local/bin curl -fsSL https://opencode.ai/install | bash
 XDG_BIN_DIR=$HOME/.local/bin curl -fsSL https://opencode.ai/install | bash
 ```
 
diff --git a/vendor/opencode/package.json b/vendor/opencode/package.json
index 71cf2817..bfd1976f 100644
--- a/vendor/opencode/package.json
+++ b/vendor/opencode/package.json
@@ -1,11 +1,11 @@
 {
   "$schema": "https://json.schemastore.org/package.json",
-  "name": "heimdall",
+  "name": "opencode",
   "private": true,
   "type": "module",
   "packageManager": "bun@1.2.19",
   "scripts": {
-    "dev": "bun run --conditions=development packages/heimdall/src/index.ts",
+    "dev": "bun run --conditions=development packages/opencode/src/index.ts",
     "typecheck": "bun run --filter='*' typecheck",
     "stainless": "./scripts/stainless",
     "postinstall": "./script/hooks"
@@ -36,7 +36,7 @@
   },
   "repository": {
     "type": "git",
-    "url": "https://github.com/sst/heimdall"
+    "url": "https://github.com/sst/opencode"
   },
   "license": "MIT",
   "prettier": {
diff --git a/vendor/opencode/packages/opencode/bin/opencode b/vendor/opencode/packages/opencode/bin/opencode
index 28fad043..8f75eb18 100755
--- a/vendor/opencode/packages/opencode/bin/opencode
+++ b/vendor/opencode/packages/opencode/bin/opencode
@@ -1,8 +1,8 @@
 #!/bin/sh
 set -e
 
-if [ -n "$HEIMDALL_BIN_PATH" ]; then
-    resolved="$HEIMDALL_BIN_PATH"
+if [ -n "$OPENCODE_BIN_PATH" ]; then
+    resolved="$OPENCODE_BIN_PATH"
 else
     # Get the real path of this script, resolving any symlinks
     script_path="$0"
@@ -32,9 +32,9 @@ else
         *) arch="$(uname -m)" ;;
     esac
     
-    name="heimdall-${platform}-${arch}"
-    binary="heimdall"
-    [ "$platform" = "win32" ] && binary="heimdall.exe"
+    name="opencode-${platform}-${arch}"
+    binary="opencode"
+    [ "$platform" = "win32" ] && binary="opencode.exe"
     
     # Search for the binary starting from real script location
     resolved=""
@@ -49,7 +49,7 @@ else
     done
     
     if [ -z "$resolved" ]; then
-        printf "It seems that your package manager failed to install the right version of the Heimdall CLI for your platform. You can try manually installing the \"%s\" package\n" "$name" >&2
+        printf "It seems that your package manager failed to install the right version of the opencode CLI for your platform. You can try manually installing the \"%s\" package\n" "$name" >&2
         exit 1
     fi
 fi
diff --git a/vendor/opencode/packages/opencode/bin/opencode.cmd b/vendor/opencode/packages/opencode/bin/opencode.cmd
index 061a672d..3a4ef3e7 100644
--- a/vendor/opencode/packages/opencode/bin/opencode.cmd
+++ b/vendor/opencode/packages/opencode/bin/opencode.cmd
@@ -1,8 +1,8 @@
 @echo off
 setlocal enabledelayedexpansion
 
-if defined HEIMDALL_BIN_PATH (
-    set "resolved=%HEIMDALL_BIN_PATH%"
+if defined OPENCODE_BIN_PATH (
+    set "resolved=%OPENCODE_BIN_PATH%"
     goto :execute
 )
 
@@ -24,8 +24,8 @@ if "%PROCESSOR_ARCHITECTURE%"=="AMD64" (
     set "arch=x64"
 )
 
-set "name=heimdall-!platform!-!arch!"
-set "binary=heimdall.exe"
+set "name=opencode-!platform!-!arch!"
+set "binary=opencode.exe"
 
 rem Search for the binary starting from script location
 set "resolved="
@@ -48,7 +48,7 @@ set "current_dir=%parent_dir%"
 goto :search_loop
 
 :not_found
-echo It seems that your package manager failed to install the right version of the Heimdall CLI for your platform. You can try manually installing the "%name%" package >&2
+echo It seems that your package manager failed to install the right version of the opencode CLI for your platform. You can try manually installing the "%name%" package >&2
 exit /b 1
 
 :execute
diff --git a/vendor/opencode/packages/opencode/package.json b/vendor/opencode/packages/opencode/package.json
index 6a997586..5635fab6 100644
--- a/vendor/opencode/packages/opencode/package.json
+++ b/vendor/opencode/packages/opencode/package.json
@@ -1,7 +1,7 @@
 {
   "$schema": "https://json.schemastore.org/package.json",
   "version": "0.4.45",
-  "name": "heimdall",
+  "name": "opencode",
   "type": "module",
   "private": true,
   "scripts": {
@@ -9,7 +9,7 @@
     "dev": "bun run --conditions=development ./src/index.ts"
   },
   "bin": {
-    "heimdall": "./bin/heimdall"
+    "opencode": "./bin/opencode"
   },
   "exports": {
     "./*": "./src/*.ts"
@@ -35,8 +35,8 @@
     "@octokit/graphql": "9.0.1",
     "@octokit/rest": "22.0.0",
     "@openauthjs/openauth": "0.4.3",
-    "@heimdall-ai/plugin": "workspace:*",
-    "@heimdall-ai/sdk": "workspace:*",
+    "@opencode-ai/plugin": "workspace:*",
+    "@opencode-ai/sdk": "workspace:*",
     "@standard-schema/spec": "1.0.0",
     "@zip.js/zip.js": "2.7.62",
     "ai": "catalog:",
diff --git a/vendor/opencode/packages/opencode/src/index.ts b/vendor/opencode/packages/opencode/src/index.ts
index 1019ed13..868c96ba 100644
--- a/vendor/opencode/packages/opencode/src/index.ts
+++ b/vendor/opencode/packages/opencode/src/index.ts
@@ -37,7 +37,7 @@ process.on("uncaughtException", (e) => {
 })
 
 const cli = yargs(hideBin(process.argv))
-  .scriptName("heimdall")
+  .scriptName("opencode")
   .help("help", "show help")
   .version("version", "show version number", Installation.VERSION)
   .alias("version", "v")
@@ -61,9 +61,9 @@ const cli = yargs(hideBin(process.argv))
       })(),
     })
 
-    process.env["HEIMDALL"] = "1"
+    process.env["OPENCODE"] = "1"
 
-    Log.Default.info("heimdall", {
+    Log.Default.info("opencode", {
       version: Installation.VERSION,
       args: process.argv.slice(2),
     })
